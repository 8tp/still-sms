# qa report — 2026-05-11

## verified

- branch: `overnight/2026-05-11-hardening`
- local command: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ./gradlew --no-daemon :app:assembleDebug :app:testDebugUnitTest --stacktrace`
- result: green
- pact check: seven SMS/MMS/contact/notification permissions only, no network-adjacent permissions, no firebase/gms/analytics dependency

## work reviewed

- root pact link added to the README
- build workflow added
- block-list matching now has pure exact-match tests for strict E.164, national/short numeric senders, and alphanumeric sender IDs
- thread export formatting now has pure tests for direction arrows, multiline flattening, and MMS image placeholders
- MMS PDU tests added for synthetic SMIL/text/tiny inline PNG and long-form content-type parsing
- MMS download handling now marks the placeholder failed and removes the staged PDU file when provider updates, part inserts, or binary streams fail
- WAP push handling ignores unexpected broadcast actions and cleans up partially staged download files when FileProvider setup fails
- minSdk lint failures from telephony feature declarations and the API 27 navigation-bar attr were addressed
- inbound SMS/MMS notifications now post only after successful provider persistence, and MMS failures stay visible as inbound failed/not-downloaded placeholders
- MMS auto-download honors the persisted mobile-data setting and checks block-list rules before provider writes and again after retrieve sender resolution
- staged MMS part rows are cleaned up if a later binary stream open/write fails
- notification and compose intents are handled through warm `onNewIntent` paths, and default-role grants force a fresh thread-list provider query

## risks

- MMS tests are synthetic JVM coverage, not carrier APN validation
- MMS provider error-path coverage is JVM and code-review coverage; the real provider path still needs device validation
- notification tap and default-role refresh behavior still need emulator validation because there are no `androidTest` UI smokes yet
- block-list entries are exact canonical keys: E.164 stays `+digits`, national/short numeric senders become digit-only, and alphanumeric sender IDs become uppercase; no prefix or fuzzy match is applied

## next improvements

- add manual tests for short-code and alphanumeric sender blocking on a real carrier line
- add docs for carrier MMS test cases and expected `MMS_ERROR_*` capture before the 1.0 carrier gauntlet
